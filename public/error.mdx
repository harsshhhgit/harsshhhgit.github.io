(function (inDevelopment) {
  function launch() {
    if (inDevelopment) {
      return;
    }
    //TODO
  }
  window.addEventListner('load', launch);
}(true));

// 404 page not found.
if (!found) {
// throw ("(╯°□°)╯︵ ┻━┻");
throw ("fuck off bitch!!!");
}
// Go home!